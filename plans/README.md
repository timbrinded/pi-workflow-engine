# Implementation Plans

Generated by the improve skill on 2026-06-16; updated on 2026-06-23 for budget review follow-ups and for the built-in Workflow-tool feature-parity set (004–007). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add workflow-level usage and cost reporting for Dynamax runs | P1 | M | — | DONE |
| 002 | Output-token budget for workflow runs | P1 | S–M | 001 | DONE |
| 003 | Harden workflow budget parsing, enforcement, and docs | P1 | M | 002 | DONE |
| 004 | Fail-soft `parallel` and `pipeline` (survivors live) | P1 | S–M | — | DONE |
| 005 | Re-prompt a schema agent that never called `final_answer` | P2 | S | — | DONE |
| 006 | Resume / journaling (`resumeFromRunId`) | P3 | L | — | TODO |
| 007 | Per-agent worktree isolation (`isolation: "worktree"`) | P2 | M–L | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 001 has no dependencies.
- 002 depends on 001 because the budget reads workflow usage/cost totals produced by 001.
- 003 depends on 002 because it hardens and documents the budget feature introduced there.
- 004–007 are the built-in dynamic-Workflow-tool **feature-parity** set (see the `feature-parity-roadmap` memory). None has a hard dependency, but the recommended order is **004 → 005 → 007 → 006**, by leverage and risk:
  - **004 first** — highest leverage, lowest risk; fail-soft combinators also stop plan 002/003's budget throw from cascading and aborting in-flight siblings.
  - **005** — small, isolated robustness win on the structured-output path.
  - **007** — unlocks mutating/parallel-edit workflows; its worktree teardown is cleaner once 004 has landed.
  - **006 last** — largest and riskiest (run identity, deterministic call keys, journaling); land 004/005 first.

## Findings considered and rejected

- Mutate host assistant `usage` to force pi footer/session stats to include subagent costs: rejected for this plan because pi uses assistant token usage for context-window estimates; mixing subagent token counts into host messages risks incorrect compaction/context display. Prefer workflow-level reporting now and a pi core extension-usage API later if footer integration is required.
