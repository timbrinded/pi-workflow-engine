{
  "name": "pi-workflow-engine",
  "version": "0.3.0",
  "type": "module",
  "description": "A customisable multi-agent workflow-orchestration engine for the pi coding agent.",
  "keywords": ["pi-package"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/timbrinded/pi-workflow-engine.git"
  },
  "files": ["src", "workflows"],
  "pi": {
    "extensions": ["./src/index.ts"],
    "image": "https://raw.githubusercontent.com/timbrinded/pi-workflow-engine/master/assets/preview.png"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun tests/run.ts",
    "test:changedlines": "bun tests/run.ts tests/changedlines.test.ts",
    "test:concurrency": "bun tests/run.ts tests/concurrency.test.ts",
    "test:smoke": "bun tests/run.ts tests/discovery.test.ts",
    "test:ui": "bun tests/run.ts tests/workflow-ui.test.ts",
    "prepublishOnly": "tsc --noEmit && bun run test"
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.0"
  }
}
